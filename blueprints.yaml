name: MX Cookieless Analytics
type: plugin
slug: mx-cookieless-analytics
version: 1.0.0
description: Cookieless, privacy-friendly web analytics. Opt-in tracking (off until you connect), GDPR-friendly, EU-hosted. Automatic site verification, no DNS records needed.
icon: chart-line
author:
  name: MetriXs Analytics
  email: info@metrixs.eu
  url: https://metrixs.eu
homepage: https://github.com/davidhildering/grav-plugin-mx-cookieless-analytics
keywords: analytics, privacy, gdpr, cookieless
bugs: https://github.com/davidhildering/grav-plugin-mx-cookieless-analytics/issues
license: MIT
compatibility:
  grav:
    - '1.7'
    - '2.0'

dependencies:
  - { name: grav, version: '>=1.7.0' }

form:
  validation: loose
  fields:
    enabled:
      type: toggle
      label: Enable tracking
      help: Tracking is opt-in. Nothing is loaded or sent until you connect the site and switch this on.
      highlight: 1
      default: 0
      options:
        1: 'Yes'
        0: 'No'
      validate:
        type: bool
    api_key:
      type: text
      label: Site API key
      help: Create one in the MetriXs dashboard under Settings → Sites → API keys. Verification starts automatically after saving.
      placeholder: mtx_live_…
    api_base:
      type: text
      label: MetriXs URL
      default: 'https://app.metrixs.eu'
      help: Only change this for a self-hosted or custom MetriXs endpoint.
    domain:
      type: text
      label: Site domain
      placeholder: example.com
      help: Leave empty to auto-detect from the site URL.
    exclude_admin:
      type: toggle
      label: Exclude logged-in admins
      help: Never count visits by logged-in admin users.
      highlight: 1
      default: 1
      options:
        1: 'Yes'
        0: 'No'
      validate:
        type: bool
    connected:
      type: hidden
    challenge:
      type: hidden
